# Copy this file, rename it .env.dev, and fill in your local credentials.
# Never commit .env.dev to GitHub!  It is listed in .gitignore.
DB_HOST=localhost
DB_PORT=5432
DB_USER=
DB_PASSWORD=
DB_NAME=pokemon
DB_SSLMODE=disable
ADDR=:8080
